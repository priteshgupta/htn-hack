'use strict';

// var sp = require('libspotify');
// var session = new sp.Session({
//     applicationKey: 'spotify_appkey.key'
// });

// session.once('login', function() {
//     console.info('Logged in to Spotify');
// });

// session.login(process.env.spotify_user, process.env.spotify_pass);

exports.searchSong = function(body, cb) {
	// var search = new sp.Search(body);
	// search.trackCount = 10; // get the 10 first results
	// search.execute(function() {
 //    console.log(search.tracks);
	// 	cb();
	// });
};
